# Environment variables for the Whiz POS Local Business Server

# -- Application Configuration --
NODE_ENV=production
PORT=4001

# -- Database & Cache --
# Connection string for the local MongoDB instance.
# Replace <BUSINESS_ID> with the actual business identifier.
MONGO_URI=mongodb://localhost:27017/whiz_business_<BUSINESS_ID>

# Connection string for the local Redis instance.
REDIS_URL=redis://localhost:6379

# -- Cloud Integration --
# URL of the central Whiz POS Cloud Portal.
CLOUD_PORTAL_URL=https://portal.whizpos.cloud

# API key for communication with the cloud portal.
# This is set after the device linking process.
CLOUD_API_KEY=

# Secret key for this specific device, generated during setup.
DEVICE_SECRET=

# Interval in seconds for syncing data with the cloud.
SYNC_INTERVAL_SECONDS=300

# -- Peripherals --
# Printer configuration.
# PRINTER_MODE can be one of: usb, network, bluetooth
PRINTER_MODE=usb
# PRINTER_DEVICE_ID depends on the mode (e.g., IP address for network, device path for usb).
PRINTER_DEVICE_ID=

# -- Security --
# Secret for signing JWTs for local POS user sessions.
JWT_SECRET=

# -- Backups --
# Directory to store local database backups.
BACKUP_DIR=/var/whiz/backups
